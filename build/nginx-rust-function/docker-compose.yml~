version: "3"
services:
  builder:
    command: "sleep infinity"
#    image: rust:buster
    image: nginx-link-function
    # ports:
    #   - '8000:8000'
    # depends_on:
    #   - ipfs
    #   - postgres
    volumes:
      - ../:/mbdn/src:rw

